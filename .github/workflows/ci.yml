name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build and test contracts
        run: |
          npm run build --workspace=packages/contracts
          npm test --workspace=packages/contracts

      - name: Build and test workspace
        run: |
          npm run build --workspace=packages/workspace
          npm test --workspace=packages/workspace

      - name: Build core
        run: npm run build --workspace=packages/core

      - name: Test core
        run: npm test --workspace=packages/core

      - name: Build excel-io
        run: npm run build --workspace=packages/excel-io

      - name: Test excel-io
        run: npm test --workspace=packages/excel-io

      - name: Build sample-data
        run: npm run build --workspace=packages/sample-data

      - name: Build and test runner
        run: |
          npm run build --workspace=packages/runner
          npm test --workspace=packages/runner

      - name: Build web-free
        run: |
          npm test --workspace=apps/web-free
          npm run build --workspace=apps/web-free
